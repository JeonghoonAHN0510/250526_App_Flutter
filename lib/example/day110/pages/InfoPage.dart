import 'package:flutter/material.dart';

class Infopage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("정보 페이지")),
      body: Text("정보 페이지 본문"),
    );
  } // build end
} // class end